# Pages

Add full-screen workflows here as the app grows. Suggested page folders:

- `map/` for the authenticated live map experience
- `auth/` for login and signup screens
- `profile/` for the crew, hospital, and traffic-control profile page

Shared UI belongs in `src/components`; API and authentication logic belongs in `src/services`.
